import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CustomButton = () => {
  return (
    <View>
      <Text style={{ color: "green" }}>This is android</Text>
    </View>
  );
};

export default CustomButton;

const styles = StyleSheet.create({});
